# Keep local-only files out of the repository, and so off the live site.

# PHP built-in server logs (php -S localhost:8001 router.php > server.log)
server.log
*.log

# OS and editor clutter
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.bak

# SMTP credentials for the enquiry form. The sample file is committed; this
# one holds the real password and must never reach the repository.
mail-config.php
